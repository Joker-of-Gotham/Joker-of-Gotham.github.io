# 列表摘要、筛选与计划导航修正

## 范围与决策

根据用户本轮反馈修正既有内容展示；本地 Feature / Bugfix，S1、QA-L2，M3 本地验证目标。Owner 为 Joker-of-Gotham，DRI 为 Codex。不涉及数据迁移、外部发布或新增内容来源，无需单独 RFC / ADR。

- 博客、研究、书评、杂谈列表及全站搜索共用构建期 Markdown 预览。采用 Astro 自身的 Markdown 处理器、remark-math、KaTeX 和 Shiki，浏览器无需增加解析器。先生成语法树，再按文本节点限制摘要长度，完整保留数学与行内代码节点；旧版被截断的摘要从正文重新提取。
- 摘要作为整张文章链接的内容，省略图片、原始 HTML、链接包裹及交互控件；正文的公式滚动增强跳过摘要。完整正文保留原有媒体、复制代码和公式滚动功能。
- 研究首页固定顺序为研究方向、研读札记、论文收藏。
- 新板块统一使用博客的搜索框、筛选折叠面板及自定义下拉样式。选项支持方向键、Home / End、Enter、Escape，选择“全部”扩大范围；条件写入 URL 并保留 Astro 历史状态，导航返回可恢复。
- 删除全部可见 Reset / 重置按钮及相应处理代码。计划页面、面包屑、返回链接、搜索索引与侧栏统一用“计划”，侧栏顺序紧邻“关于”之前。现有 `/roadmap/` 链接继续有效。

## 验证

实际执行结果（2026-09-09）：

- `npm run check`：0 errors、0 warnings、3 条原有 hints。
- `npm run test`：16 个文件、75 项单元测试全部通过；包含加粗/列表/代码/公式、旧摘要恢复、公式边界及原始 HTML 排除。
- `npm run build` 与 Pagefind：成功，82 页。
- `npx playwright test tests/e2e/content-polish.spec.ts tests/e2e/library.spec.ts tests/e2e/archive.spec.ts tests/e2e/sidebar.spec.ts`：首轮 33 通过、1 失败。新增的跨列表一致性检查发现正文脚本仍在给摘要公式添加 `tabindex`；修正该实际问题后重新构建。
- 在最终构建上重跑本轮全部 4 项浏览器用例及正文公式/任务列表键盘用例：5/5 通过。34 个相关独立用例均已取得通过结果；不是声称首轮全绿。
- 覆盖研究排序、计划命名和侧栏顺序、全部按钮中无 Reset / 重置、同一真实图论文章在书籍/博客/搜索中的 HTML 一致性、筛选返回恢复、明暗主题展开筛选的键盘交互和 WCAG A/AA 自动检查。
- 新板块在 320/768/1280px 检查无页面横向溢出；侧栏 16 个页面 × 2 个宽度 × 2 种主题，共 64 组开关及实际点击通过。正文媒体和原有公式键盘滚动仍正常。
- Codex 浏览器检查图论书评列表、展开的书架筛选和研究首页全页；另查看 320px 暗色筛选截图，确认布局、配图、摘要与顺序。
- `git diff --check` 通过。自动检查不代表真实设备测试；修改仅保存在本地，未提交、推送或部署。

## 发布授权 · 2026-09-09

完成上述验证后，Owner 明确要求执行 add、commit 并推送远端 main。此次为已有改动的发布 Operation，S1 / QA-L2；Owner：Joker-of-Gotham，DRI：Codex。发布范围包含本轮三个内容板块、论文卡片与配图、侧栏修复、Markdown 摘要和筛选统一，以及对应说明与测试；沿用上述实际验证结果。采用普通 Git 推送，推送后核对远端 main 与本地提交一致。Git 推送成功不等同于 GitHub Pages 部署完成。
