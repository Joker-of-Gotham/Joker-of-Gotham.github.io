# Lunar Signal Observatory Design System

> **状态**：Approved · 2026-08-28<br>
> **产品 Owner**：Joker-of-Gotham（品牌与发布最终审批）<br>
> **设计 / 实现 DRI**：当前博客前端升级任务<br>
> **治理级别**：公开站点重构 Project · S2 · 目标 QA-L3 · 当前 M3<br>
> **运行时令牌事实源**：[`src/styles/tokens.css`](src/styles/tokens.css)<br>
> **实现规约**：[`design-system/MASTER.md`](design-system/MASTER.md)

本文件是视觉、交互和界面决策的最高事实源。若页面实现、旧文档或局部样式与本文件冲突，以本文件为准；任何新增视觉值必须先进入本文件及令牌系统，再进入组件。

## 1. Overview

### 1.1 品牌结构

博客不是“套了动画的文章列表”，而是一座可被探索、可被阅读、也可被长期维护的数字观测站。统一品牌由三层构成：

1. **B · Lunar Signal Observatory（月面信号观测站）**：主品牌与第一视觉印象。承担首页、重大成果和空间叙事，关键词是月面、轨道、信号、棱晶、雾与深空尺度。
2. **A · Nocturne Research Archive（夜曲研究档案）**：阅读与检索系统。承担 Blog、Search、Roadmap、文章页，关键词是清晰、安静、可信、可扫描和长时间阅读。
3. **C · Afterlight Band-Signal Theatre（余辉乐队信号剧场）**：个人彩蛋层。承载日常番、百合番、乐队番的情感气质，使用克制的粉色余辉、节拍和“记忆切片”，不能压过研究内容。

### 1.2 美学哲学

- **Essence**：以研究者的精确组织电影化的二次元宇宙，让技术内容像被观测、编目和重新点亮的信号。
- **Keywords**：cinematic、lunar、precise、tender、layered。
- **Material**：深空雾场、开放式内容平面、局部观测玻璃、金属轨道、发光信号和纸张式阅读面。
- **Temperature**：深色冷静，浅色带月纸暖意；玫瑰余辉只提供情感温度。
- **Density**：入口空气感强，档案适中，正文低密度。
- **Motion**：入口表现力强，阅读区克制，所有持续运动均可停用。

### 1.3 核心原则

- **内容永远可达**：WebGL 是空间层，不是语义层。标题、导航、正文、表单和搜索必须保留在 DOM。
- **震撼来自编排**：用尺度、景深、留白、光线和节奏制造冲击，不靠同时堆叠所有特效。
- **三层一体**：主品牌、阅读系统和个人彩蛋共享同一色彩、字体、形状与动效语法。
- **双主题同等设计**：浅色不是深色反相，而是“月纸观测室”；两种主题都必须满足阅读、图像和角色可辨识性。
- **退化仍然完整**：无 WebGL、低性能、节能或减少动态时，静态 Poster 与完整 DOM 仍能表达同一叙事。

### 1.4 页面动效强度

| 页面族 | 视觉层 | 默认动效强度 | 内容优先级 |
|---|---|---:|---:|
| 首页 / 重大成果 | B + C | 高（可降级） | 高 |
| Artifacts / Roadmap | B + A | 中 | 很高 |
| Blog / Search | A | 低至中 | 最高 |
| 普通文章 | A，局部 B | 低 | 最高 |
| 彩蛋片段 | C | 短时高、常态低 | 中 |

## 2. Colors

### 2.1 基础色锚点

所有基础颜色使用 OKLCH；注释中的十六进制仅用于与批准的视觉 Brief 对照，不得在组件中使用。

| 名称 | OKLCH | Brief 对照 | 角色 |
|---|---|---|---|
| Void | `oklch(0.1229 0.0108 271.39)` | `#05060A` | 深空底层 |
| Ink | `oklch(0.1604 0.0152 272.20)` | `#0B0D14` | 深色页面底 |
| Moon Paper | `oklch(0.9618 0.0086 84.57)` | `#F5F2EC` | 浅色页面底 / 深色前景 |
| Signal Violet | `oklch(0.6642 0.1875 286.04)` | `#8D7CFF` | 主操作、角色信号、关键节点 |
| Orbit Blue | `oklch(0.7240 0.1439 258.69)` | `#6CA6FF` | 链接、轨道、信息状态 |
| Afterlight Rose | `oklch(0.7371 0.1159 359.13)` | `#E58AA8` | 个人彩蛋、情感余辉 |
| Observatory Gold | `oklch(0.7494 0.0826 84.76)` | `#C6AA70` | 金属刻度、警告、稀有强调 |

中性色承担至少 80% 的可见表面；Violet 与 Blue 合计不超过常态视口的 15%；Rose 与 Gold 合计不超过 5%。动漫图片本身可以有高色彩密度，但必须通过遮罩、裁切和周围留白进入统一语境，不能让 UI 跟随每张图片临时换色。

### 2.2 语义映射

| 语义 | 深色 | 浅色 | 用途 |
|---|---|---|---|
| `background` | Ink | Moon Paper | 页面基底 |
| `background-deep` | Void | warm paper 100 | Canvas / 章节过渡 |
| `surface` | ink 800 | white | 卡片、面板 |
| `foreground` | Moon Paper | Void | 标题、主要文本 |
| `foreground-soft` | mist 300 | ink 750 | 正文 |
| `muted` | mist 500 | ink 600 | 元数据、说明 |
| `primary` | Violet 500 | Violet 700 | 关键操作、当前信号 |
| `secondary` | Blue 500 | Blue 700 | 链接、轨道、信息 |
| `accent` | Rose 500 | Rose 700 | Afterlight 彩蛋 |
| `metal` | Gold 500 | Gold 700 | 刻度、警告、微型标识 |

组件只允许消费语义或组件令牌。状态颜色必须同时具有文字/图标/形状提示，不得只依赖色相。焦点环必须在两种主题下清晰可见。

### 2.3 对比与图像规则

- 正文与背景目标至少 WCAG 2.2 AA `4.5:1`；大号文字至少 `3:1`；可交互边界和焦点指示至少 `3:1`。
- 彩色小字不得直接使用明亮品牌色；使用语义前景色，并把品牌色留给标记、边框或背景。
- 动漫截图保留为“信号窗口 / 记忆切片 / 档案投影”，但在素材来源解决前属于发布门禁，详见 [`docs/asset-provenance.md`](docs/asset-provenance.md)。
- 渐变最多使用两个主色锚点加一个中性色；禁止彩虹渐变和无语义霓虹。

## 3. Typography

### 3.1 字体角色

| 角色 | 字体栈 | 使用范围 |
|---|---|---|
| Display | Space Grotesk → Source Han Serif SC / Noto Serif CJK SC | 英文展示字、中文章节题、电影式数字 |
| Body / UI | Inter → Source Han Sans SC / Noto Sans CJK SC / 系统中文 | 正文、导航、表单、说明 |
| Mono | JetBrains Mono → SFMono / Consolas | 代码、状态、坐标、时间与数据 |

Display 栈让拉丁字符保持几何观测仪器感，同时让中文回退到宋体式叙事质感。一个视口最多出现常规、半粗、粗体三种重量；正文不使用细字重。

### 3.2 流体字号

全部字号由 `--text-xs` 至 `--text-5xl` 的 `clamp()` 令牌提供：

- `xs/sm`：元数据、观测编号、短标签；
- `base/lg`：正文、导语和交互控件；
- `xl/2xl/3xl`：卡片标题、章节标题、页面标题；
- `4xl/5xl`：首页主标题和重大成果标题，仅在有足够留白时使用。

正文行高 `1.75`，标题 `1.08–1.2`，代码 `1.65`。正文理想行长为 60–78 个拉丁字符或约 30–42 个汉字；数值表格、计时和坐标启用 tabular numbers。

### 3.3 排版层级

- 首页 H1：Display Bold，允许两行，必须保留一个清晰的动词或研究主张。
- 页面 H1：Display Bold，不用全大写中文；英文 kicker 可大写并增加字距。
- 正文 H2/H3：保持左对齐；使用细线、序号或轨道节点组织，不使用每节不同装饰。
- 引语：中文可使用 Display 栈的 CJK serif 回退；引用来源仍用 Body。
- 代码：绝不缩小到低于 `--text-sm`；长行优先横向滚动而非压缩。

## 4. Layout

### 4.1 尺度与网格

- 基础间距为 4pt，所有常规间距使用 `--space-*`。
- 阅读列最大宽度 `44rem`；常规内容区 `75rem`；电影化场景上限 `100rem`。
- 页面水平边距使用 `clamp(1rem, 3.2vw, 3.5rem)`；章节纵向节奏使用 `clamp(4rem, 9vw, 9rem)`。
- 桌面常规为 12 列，平板 8 列，移动端 4 列；布局判断以内容拥挤程度为准，不以设备品牌为准。

### 4.2 页面模型

**首页**采用连续空间分镜，不使用永久固定侧栏。DOM 章节顺序固定为 Signal Gate → Observe → Structure → Orchestrate → Embodiment → Archive / Afterlight；相机运动服务于这条顺序，但滚动本身不得被锁死。

**Blog / Search / Roadmap**采用档案工作台：轻量悬浮导航、明确筛选器、可扫描结果区。可有低强度背景信号，但不得在输入、阅读或键盘导航时制造干扰。

**文章页**采用稳定阅读列，可外扩宽图、图表和交互岛。目录、边注和阅读进度是辅助，不得挤压正文至小于建议行长。

### 4.3 密度规则

- 首屏只表达一个主张、一个主要动作和一个探索提示。
- 卡片网格同一行最多三种信息层级；元数据不得与标题争夺对比度。
- 电影化大留白只出现在章节转折；连续两个纯装饰屏视为浪费。
- 长文每 3–5 个屏幕应出现一个结构锚点：小标题、图表、摘要或引用。

## 5. Elevation & Depth

### 5.1 深度层级

| 层 | 表现 | 示例 |
|---|---|---|
| 0 · Void | 无阴影、雾与星尘 | 页面 / Canvas 最底层 |
| 1 · Surface | 极轻边框与 `shadow-sm` | 文章卡、代码块 |
| 2 · Instrument | `shadow-md` + 受控玻璃 | 导航、筛选、浮层 |
| 3 · Focus | `shadow-lg` + 焦点环 | 对话框、命令面板 |
| 4 · Signal | 光场而非更大阴影 | 角色轮廓、关键成果 |

玻璃不是默认材质。只有内容后方确实存在有意义的空间或图像时才使用 `backdrop-filter`；正文主表面必须保持稳定、近乎不透明。一个组件最多一个外阴影和一个内高光。

### 5.2 WebGL 与 DOM 深度

Canvas 固定为视觉背景层，DOM 内容层永远拥有可读遮罩与清晰交互。环境星尘不得穿过关键正文形成高频闪烁；指针交互区必须显式限定，不能让全屏 Canvas 吞掉链接、选择文本或滚动。

## 6. Shapes

- `xs (6px)`：微型状态块、代码内标签。
- `sm (10px)`：按钮、输入框、小标签。
- `md (16px)`：常规卡片、图片窗口。
- `lg (24px)`：浮层、大卡、章节观测窗。
- `xl (32px)`：只用于大幅叙事容器。
- `orbit`：头像、状态点、轨道胶囊。

形状语言是“圆角仪器 + 精密轨道”，不是全站药丸化。相邻容器的圆角差至少一级；图片圆角应跟随承载容器。轨道线宽通常为 1px，当前信号节点可为 2px；不要使用锯齿状赛博边框或装饰性切角。

## 7. Components

### 7.1 导航

- 首页使用轻量悬浮导航：品牌、当前章节、声音、主题和全局搜索是上限。
- 阅读页允许持久导航，但默认收敛，键盘焦点进入后完整展开。
- 当前项同时使用位置、文字权重与信号点表示；不得只改颜色。

### 7.2 按钮与表单

- 每个作用域只有一个 Primary。Primary 使用 Violet；Secondary 使用稳定表面；危险操作使用 Danger。
- 所有控件具备 default / hover / focus-visible / active / disabled / loading 状态。
- 移动端触控目标至少 `44 × 44px`；焦点环不可被阴影或 overflow 裁切。
- 表单错误在字段附近提供文字说明，不只使用红色边框。

### 7.3 卡片与档案条目

- **闭合卡片不是默认容器**。优先使用开放式内容平面、局部边缘、轨道线、留白和排版层级组织关系；只有可拖动、可选中或需明确封闭语义的对象才使用完整边框。
- 同一视口不得出现三层以上闭合容器嵌套；子内容优先共享父级背景，以短边线、切线、光轨或错位建立层次。
- 档案条目以内容关系而非统一高度组织；封面、标题、摘要、元数据遵循固定顺序。
- Hover 位移不超过 `--motion-distance-sm`；移动端不依赖 hover 才能显露信息。
- 动漫封面作为档案投影：使用统一比例、焦点裁切和主题遮罩，不随意添加额外贴纸或多彩描边。

### 7.4 文章系统

- 正文、标题、代码、表格和图注使用 Article 组件令牌。
- 宽图 / Mermaid / 数学公式可以突破阅读列，但必须在移动端回到安全宽度。
- 代码块、引用和提示框按语义区分；色彩只作为第二通道。

### 7.5 多姿态 2.5D 虚拟观测员

- 角色为明确成年的原创女性观测员：中长发、可爱女性化学院制服、短百褶裙、接近膝盖的长袜、皮鞋；深浅形态保持同一身份、脸型、姿态与制服结构。
- 深色形态以 navy / black、Violet / Rose 信号边为主；浅色形态以 moon white / mist gray、同一信号边为主。
- 深浅主题各使用一套严格对齐的 4×2 姿态图集；正面待机、三分之二转身、侧身迈步、回身望向用户、指向、展开掌心、快速转身与收束姿态必须拥有真实不同的肢体轮廓，不得再让一张正面图承担大角度转身。
- `PoseDirector` 以当前/下一姿态、权重、方向和滞回区间组织正向与反向滚动；跨度较大的姿态交换应借助门柱、桁架、雾层或前景掠过隐藏双影。
- 呼吸、重心、视线、发梢和裙摆延迟是叠加动作，只能在小范围内变形；章节动作与相机、局部信号响应共同编排。
- 深浅形态在同一姿态锚点上交叉淡变；位置、尺度与接触阴影必须证明角色处在场景中，不能像独立贴纸悬浮。
- 当前实现属于项目自有的 **Three.js multi-pose 2.5D actor**，不是 Cubism Live2D、VRM、骨骼模型或完整体积角色；在没有合法分层模型与运行时许可前不得作此类宣称。
- 竖屏没有可靠的横向角色安全区：`≤ 820px` 竖屏保留 Three.js 世界，但不初始化实时角色；六章改用内容流内的同图集姿态舞台。它们必须显示完整可辨的章节动作、不得压住文字，并在转为横屏时自动重挂载实时角色。
- Reduced Motion / Save-Data 的竖屏舞台只复用单张 Poster；不得请求 controller 或姿态图集。普通竖屏只加载当前主题的一张图集，禁止 CSS 与 TextureLoader 并发重复下载。
- 角色只在首页和高强度成果场景保持实时动作；阅读页使用静态海报或极低频局部动作。

### 7.6 声音控制

- 页面默认静音且不得预播放；只有用户主动开启后才加载 / 播放环境音。
- 控件必须显示静音、加载、播放、错误和音量状态，并提供立即关闭方式。
- 选择可本地记忆；移动网络、节能模式和减少动态模式下默认不加载。

### 7.7 场景容器

- 一个共享 Three.js renderer 负责相机、雾、光场、连续巨构、环境星尘、角色网格和主题形态转换；固定世界不得随章节整体平移或重建。
- 任何场景都必须有静态 Poster、加载状态和 WebGL context loss 回退。
- 连续世界至少包含可识别的月面裂谷、分层观测阵列、穹顶/桥/拱架组成的观测城、具有桁架/面板/维护舱的轨道巨构、天体、赛博峡谷与档案盆地；六章节是同一世界内 300–500 单位距离的镜头航行，不是六个互相卸载的背景。
- 远景允许使用深浅主题严格对齐的原创电影化场景板，以低成本提供山体、城市与轨道巨构的高频细节；场景板必须位于透明 Canvas 下方，不承载文字语义，并由 Three.js 前中景、雾、遮挡、角色和章节视差证明其属于同一可航行空间。
- 相机位置与观察目标使用两条独立曲线，并由章节控制 FOV、roll、雾和光；宏大揭示优先通过真实地形/建筑遮挡、前景掠过和空气透视完成。
- 建筑 primitive 只能作为组合构件，不能裸露为主轮廓；重复桁架、灯、岩块和远城单元必须实例化，bloom 只作用于信标、窗带、轨道能量脊与档案光井。
- Canvas UI Particle Object 不再作为角色呈现方案；环境粒子仅用于尺度与空气感。正式实现遵循渲染 ADR 与依赖许可证门禁。

## 8. Do’s and Don’ts

### Do

- 先选语义 / 组件令牌，再写组件样式。
- 用相机尺度、雾密度和章节光线表达叙事转折。
- 让动漫图片成为统一系统中的“信号内容”，保留原图焦点并提供描述。
- 为鼠标、触摸、键盘、减少动态和无 WebGL 提供同等可达路径。
- 在真实中文长标题、代码、图表和弱网条件下验证组件。
- 新增资产时立即更新素材台账。

### Don’t

- 不在组件中写原始 hex/rgb、任意字号、任意间距或未登记阴影。
- 不把正文、导航或搜索绘制进 Canvas。
- 不用层层闭合卡片遮住背景；先以排版、留白、局部边缘和动线建立关系。
- 不自动播放声音，不用滚动劫持制造“电影感”。
- 不同时叠加 bloom、噪点、色差、景深、故障和粒子爆炸。
- 不把浅色主题实现成滤镜反相，不为两套主题复制组件。
- 不在来源未解决时把动漫截图或第三方代码标记为已授权。

## 9. Motion & Transitions

### 9.1 动效语法

| 令牌 | 时长 | 用途 |
|---|---:|---|
| instant | `0ms` | 布尔状态与无动画回退 |
| fast | `120ms` | hover、微反馈、tooltip |
| normal | `220ms` | 菜单、焦点、局部显隐 |
| slow | `420ms` | 卡片转场、章节进入 |
| cinematic | `900ms` | 角色姿态、相机与主题形态转换 |

微交互采用 `ease-out`，状态交换采用 `standard`，退场采用 `ease-in`，仅小尺度弹性反馈使用 `spring`。持续漂移必须低频、低振幅且可暂停；不允许每秒超过三次的闪烁。

### 9.2 叙事规则

- 入场：Poster 先可见，DOM 主张先可读，随后才渐入实时场景。
- 滚动：相机响应滚动进度，但页面原生滚动距离和惯性保持不变。
- 角色：动作从重心开始，再传递至躯干、发梢和衣摆；所有姿态使用阻尼插值，避免机械同步与突然跳帧。
- 主题切换：场景色温先过渡，角色两套纹理在同一姿态上交叉淡变，DOM 色彩同步但不闪白。
- 章节交接：前景结构先让出阅读动线，中景建筑/轨道建立方向，最后由远景天体完成尺度变化；相机、角色位置与文字入场必须共享同一节奏点。
- 离屏 / 后台：停止 requestAnimationFrame、音频和高频观察器。

### 9.3 减少动态

`prefers-reduced-motion: reduce` 或 `data-motion="reduced"` 时：持续渲染与视差系数为 0，位移距离为 0，时长折叠到 1ms；以静态 Poster、即时状态交换和必要的颜色变化替代。信息、顺序与操作能力不得减少。

## 10. Responsive Behavior

| 范围 | 导航 | 场景 | 内容 |
|---|---|---|---|
| `< 480px` | 双层紧凑仪器栏 | Low Three.js 世界 + 内容流姿态舞台；静态档复用 Poster | 4 列，单列流，紧凑元数据 |
| `480–820px` 竖屏 | 紧凑悬浮栏 | Low 世界 + 每章姿态舞台；不初始化实时角色 | 4 / 8 列，宽图贴安全边距 |
| `821–1023px` 或横屏 | 8 列工作台导航 | 标准实时角色、低密度世界 | 筛选可换行，目录按需展开 |
| `1024–1439px` | 完整导航 | 标准场景 | 12 列，阅读列 + 边注 |
| `≥ 1440px` | 完整导航 + 章节坐标 | 电影化构图，DPR 仍封顶 | 最大宽度控制，不无限拉伸 |

断点是验收范围而不是在组件里复制视觉值。布局优先使用 `minmax()`、`clamp()`、容器查询和内容驱动换行。横竖屏切换不能重载页面或丢失阅读位置。触摸设备不启用依赖精确鼠标位置的角色注视或视差；短横屏按可用高度收敛角色和巨构尺度，不得遮挡主操作。

## 11. Dark Mode Strategy

- 深色是品牌默认主题，名称为 **Void Observatory**；浅色名称为 **Moon Paper Archive**。
- Layer 1 原始色永远不随主题变化；主题只重映射 Layer 2 语义色，Layer 3 组件自然继承。
- 未显式选择时跟随 `prefers-color-scheme`；显式 `data-theme="dark|light"` 优先，并可在本地保存。
- 深色不是纯黑大面积压缩：Void、Ink、Surface 至少形成三级深度。浅色不是纯白：Moon Paper 提供暖色基底，白色仅用于抬升表面。
- 主题切换必须同步 DOM、Canvas 雾色、信号色和角色深浅形态；加载失败时至少保证 DOM 与静态 Poster 一致。
- 图像遮罩、代码高亮、Mermaid、KaTeX、评论组件和原生表单均需独立验证两种主题。

## 12. Agent Prompt Guide

实现或修改本项目界面时，Agent 必须采用以下约束：

> 你正在维护 Lunar Signal Observatory。保持 B 主品牌、A 阅读系统、C 个人彩蛋的层级。先阅读 `DESIGN.md`、`design-system/MASTER.md` 和 `src/styles/tokens.css`。所有颜色、字体、间距、圆角、阴影和动效必须使用 Layer 2 语义令牌或 Layer 3 组件令牌；禁止组件直接读取 `--primitive-*`。DOM 承载内容与交互，WebGL 只承载空间视觉。保留现有动漫图片，但检查 `docs/asset-provenance.md` 的发布门禁。声音默认静音。任何动效都要有减少动态与静态 Poster 回退。不要改变内容架构，除非任务明确授权。

每次变更按以下顺序执行：

1. 判断属于 B、A 或 C 中哪一层，以及目标页面的动效强度。
2. 查找现有语义或组件令牌；没有合适令牌时，先更新本文件与 `tokens.css`。
3. 保证 dark / light / reduced motion / no-WebGL 四种状态有定义。
4. 使用真实内容检查中文换行、键盘焦点、触摸目标、图像替代文本和读取顺序。
5. 仅报告实际执行的构建、检查、性能或视觉测试；未执行的一律标为未验证。
6. 新资产、新依赖或复制的第三方实现必须先更新 provenance / ADR，再进入发布分支。
